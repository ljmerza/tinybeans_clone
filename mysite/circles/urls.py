"""URL configuration placeholder for the circles app."""

from django.urls import include, path

# Actual URL patterns will migrate here; for now we keep them defined in
# `mysite.users.urls` and simply expose an empty namespace.
app_name = 'circles'
urlpatterns = [
    # path('', include(...)),
]

